{erl_opts, []}.

{deps, [
   datum
,  erlcloud
,  {pipe, "2.0.1", {pkg, pipes}}
,  {feta, ".*",
      {git, "https://github.com/fogfish/feta", {branch, master}}
   }
]}.

{profiles, [
   {test, [{deps, [meck]}]}
]}.
